export type ListingDetails = {
    tokenAddress: string;
    tokenName: string;
    tokenId: string;
    seller: string;
    price: string;
}

export type NFTDetails = {
    tokenAddress: string;
    tokenName: string;
    tokenId: string;
    owner: string;
    isListed: boolean;
}